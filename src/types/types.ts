export interface ArrowProps {
  side?: string;
  color?: string;
  slide?: number;
}

export type sliderItem = {
  id: number;
  img: string;
  title: string;
  desc: string;
  bg: string;
};

export type category = {
  id: number;
  img: string;
  title: string;
};
